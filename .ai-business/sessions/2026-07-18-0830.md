# Session 2026-07-18-0830

## Objective
Phase 0 intake → control system → first execution batch (foundation: security, SEO, analytics, CI).

## Work performed
- Full repo + access audit (product diagnosis in STRATEGY.md).
- Restored Convex access (`npx convex login`, device flow). Disproved "broken deploy key" hypothesis: Vercel's CONVEX_DEPLOY_KEY is valid — the earlier 401 came from `export $(grep ...)` mangling a value containing `|`/quotes.
- Baseline from prod DB: 1 seed user, 1 company, 2 modules → $0 ARR, 0 real users.
- Created `.ai-business/` control system + `.claude/skills/grow-to-1m-arr` skill.
- Batch 1 shipped (pushed to main → Vercel prod deploy):
  - `convex/r2.ts`: getSignedUrl now requires companyId, enforces owner/member/public access, and verifies the key belongs to the company's modules.
  - Deleted unused `SignInForm.tsx` (unregistered password/anonymous providers).
  - SEO baseline: meta description, OG/Twitter cards (billboard.png), favicons, per-route titles (gallery, public kit), sitemap.xml, robots.txt Sitemap line. Wired unused ErrorFallback into Sentry ErrorBoundary.
  - PostHog analytics (personal org "Brandkite", project 518164): env-gated `src/lib/analytics.ts`; events company_created, module_generation_started, checkout_started, plan_purchased, public_kit_viewed; identify on login/reset on logout; Umami removed. VITE_POSTHOG_KEY set in Vercel prod+preview and .env.local.
  - CI: `.github/workflows/ci.yml` — blocking build, informational biome+tsc (pre-existing debt). First run failed (missing pnpm version) → pinned v10.
- VISION.md: owner's "idea → launch-ready company" direction, 5-rung ladder, sequencing.

## Checks run
- `pnpm build` green locally (before and after changes). No tsc/biome errors in changed files (pre-existing debt elsewhere; quality CI job is informational).

## Decisions
- See DECISIONS.md (ICP, stack, PostHog, Autumn, prices, prod-as-sandbox with expiry).
- Commit granularity note: public/c/$id.tsx analytics landed inside the SEO commit (references analytics.ts committed one commit later) — harmless for CI (runs per push), noted for bisectability.

## Costs
$0 new spend.

## Risks introduced
- Analytics adds client JS (posthog-js) — watch bundle/latency later.
- getSignedUrl signature change is breaking for any external caller (none known; only LogoModule uses it — updated).

## Blockers
- Chrome extension not connected → no live funnel walkthrough yet.
- Autumn dashboard unverified.

## Exact next step
1. Confirm Vercel prod deploy + CI green; verify meta tags and PostHog events on brandkite.co (curl + PostHog query).
2. E1: Autumn checkout end-to-end test (BACKLOG #6).
3. Funnel walkthrough (#7) once browser access exists, else via curl/local run.
