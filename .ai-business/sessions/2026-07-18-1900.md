# Session 2026-07-18-1900 (v2 build: identity quality + publishable site)

## Objective
Codify v2 direction; execute #1 (identity quality) + #2 (publishable site) autonomously toward production. Parallel agent building the DESIGN-ENGINE (convex/lib/design/) in tandem — incorporate when it lands.

## Shipped (committed + pushed; verified live in Chrome)
- **v2 direction codified**: V2-DIRECTION.md (brand & identity layer for the AI-native company; wedge = great identity + publishable site; moat = quality + consistent source of truth; arc = brand-as-API), V2-ROADMAP.md. STRATEGY/VISION point at it. Inspired by cofounder.co (own brand deeply, don't fight horizontal breadth).
- **DESIGN-ENGINE.md** (authored by parallel agent) committed as the mechanism: diverge→checks→render→pairwise jury→novelty gate; 3 surfaces (module workflows, Brandkite MCP/brand-as-API, dogfooded site tokens). Validated technique (75% pairwise win).
- **Identity quality (#1)**: multi-method logo ensemble — 4 concepts/run (2 Recraft V4.1 + 2 LLM-authored geometric/monogram SVG via grok-4.3, tight centering), transparent bg (strip Recraft white rect), concept-picker UI + batch signed-URL query. Verified across brands (padlock/shield/monogram — all on-brand, editable).
- **Publishable site (#2, FLAGSHIP)**: real hosted on-brand landing page at `/s/:slug` from the brand (logo, palette → CTA/accents, brand Google fonts, website copy → hero/features/CTA), "Made with Brandkite" footer loop. Backend convex/site.ts (publish/unpublish/slug/getSiteBySlug) + sitePublished/siteSlug on companies + owner Publish-site control in studio. Verified live at /s/brandkite — looks professional.

## Method
Local dev + Chrome loop; empirical logo QA (rsvg render + eyeball); agents for taste-critical UI (site render), me for backend/sensitive. Incremental commits.

## Coordination
Another agent works the repo in tandem (design engine). Rebase-pull before pushing; fold its convex/lib/design/ + MCP work into commits when it lands.

## Next (V2-ROADMAP)
- Phase B design engine: jury/critique + novelty gate on logo (parallel agent).
- Phase 4: pricing around publishing (Free preview / Pro publish) via Autumn; VERIFY checkout collects (test key).
- Phase 5 / Phase C: brand-as-API (MCP server over Convex HTTP) + design-token export — the scale/infra layer.
- Front-door reshape (identity+site first, demote strategy filler); owner unblocks (Google OAuth creds, checkout, ad budget).
