# Findings — 2026-07-18 (billing verification + funnel walkthrough)

## Corrected baseline (supersedes "0 users / $0")
Autumn (billing) is the reliable signup signal — a customer is created when a signed-in user first hits a gated action. Full customer list pulled 2026-07-18:

- **13 total signed-up customers** (not 0). ~7 of 13 have consumed credits → real activation happened.
- **12 on Free**, **1 reached Pro Annual ($300/yr)**.
- **$0 actually collected.** ARR is still effectively $0 — see the critical billing finding below.
- Convex prod: ~13–19 users, ~14 companies, ~14 brandModules (CLI count approximate; Autumn 13 is the trustworthy figure). The earlier "1 user" reading was a CLI pagination artifact — **corrected**.

## CRITICAL: payment is never actually collected
The one Pro-Annual customer (`kn76nae4…`, Stripe `cus_TR1lPYGljGO5la`):
- Was **granted full Pro access** — 500 credits, `multiple_companies` flag — and has used 8 credits.
- Has **two unpaid invoices**: pro-annual **$300 status `open`** (finalized, unpaid) and pro **$30 status `draft`** (abandoned). Subscription `past_due: true`.
- Customer email is an `@invoices.useautumn.com` placeholder → checkout ran in **invoice/send-invoice mode**, not card-charge. Access was provisioned **before** payment.

**Implication:** the money-collection path is broken for self-serve SaaS. Anyone can "upgrade" and receive paid features against an unpaid invoice. This is the #1 monetization blocker — a real buyer wanted Pro but no card was ever charged.

**Recommended fix (APPROVAL REQUIRED — billing config):** switch the paid-plan attach to card-required Stripe Checkout (charge before/at grant), i.e. ensure `attach` opens a card checkout rather than emitting a send-invoice. Verify whether invoice mode is set in the Autumn dashboard org settings or passed at attach in `convex/autumn.ts` / checkout-dialog. Until fixed, "conversions" won't collect revenue.

## Products/features config: correct, matches code
- Features: `credits` (metered consumable), `multiple_companies` (boolean) — exactly what code references (`convex/track.ts`, `app-sidebar.tsx`).
- Plans: `free` (5 credits, one-off, auto-enable), `starter` $10/mo & `starter-annual` $100/yr (100 credits/mo), `pro` $30/mo & `pro-annual` $300/yr (500 credits/mo + multiple_companies). IDs match `src/components/billing.tsx` exactly.
- Note: Free grants **5 one-off** credits (no reset) — a hard lifetime cap of 5 generations. Very tight given ~28 module types; likely throttles activation before value is felt.

## Funnel walkthrough (brandkite.co, live)
1. **Signup wall at the first action.** Clicking "Create Brand" immediately opens a **magic-link-only** sign-in dialog ("Send sign-in link"). No Google/OAuth, no try-before-signup, no anonymous mode. Magic link forces leaving the site → inbox → return: the single biggest activation-drop risk. **Highest-priority conversion fix.**
2. **Gallery has 1 entry** (the Brandkite seed). No social proof, no SEO surface, and the viral loop (public kit → signup) has nothing to run on yet.
3. Landing page renders the Brandkite brand kit itself (decent implicit demo) but the only CTA walls you.
4. New SEO title/meta verified live; PostHog beacon confirmed firing (`config.js` 200, recorder loaded). No console errors on load.

## Immediate implications for the plan
- **New top priority: fix payment collection** (approval-gated) — a validated buyer exists; capture the next one.
- **Reduce signup friction**: add Google OAuth and/or a no-signup demo/try flow before the magic-link wall. Measure signup drop-off now that PostHog is live.
- **Reconsider Free = 5 lifetime credits**: likely too low to reach the "wow"; test 5/day or a higher one-off grant.
- Seed the gallery and wire the public-kit → signup loop (share button now shipped this session).
